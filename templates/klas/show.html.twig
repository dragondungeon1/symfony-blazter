{% extends 'base.html.twig' %}

{% block title %}Klas{% endblock %}

{% block body %}
    <h1>Klas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kla.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kla.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('klas_index') }}">back to list</a>

    <a href="{{ path('klas_edit', {'id': kla.id}) }}">edit</a>

    {{ include('klas/_delete_form.html.twig') }}
{% endblock %}
