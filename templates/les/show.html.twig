{% extends 'base.html.twig' %}

{% block title %}Les{% endblock %}

{% block body %}
    <h1>Les</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ le.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ le.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('les_index') }}">back to list</a>

    <a href="{{ path('les_edit', {'id': le.id}) }}">edit</a>

    {{ include('les/_delete_form.html.twig') }}
{% endblock %}
